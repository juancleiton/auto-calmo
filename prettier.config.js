module.exports = {
  // Quero aspas simples nas strings
  // Ex: import express from 'express' ao invés de "express"
  singleQuote: true,

  // Por vírgula sempre nos últimos elementos do objeto
  // Ex: conts obj = { msg: 'Hello', error: false, } ao invés de error: false
  trailingComma: 'all',

  // Retirar parênteses quando usarmos arrow function
  // Ex: value => { return a + b; } ao invés de (value) => { return a + b }
  arrowParens: 'avoid',
};
